<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zeroone.star.moneytransfer.mapper.AllotMapper">
    <insert id="insertBackId" useGeneratedKeys="true" keyProperty="id">
        insert into allot(id,frame, time, number, total, people, file, data, more, examine, user)
        values(#{id},#{frame}, #{time}, #{number}, #{total}, #{people}, #{file}, #{data}, #{more}, #{examine}, #{user})
    </insert>
</mapper>
